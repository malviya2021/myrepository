// office closed or not
var hour = 12;
var isweekend = true;

if (hour < 10 || hour > 18 || isweekend) {
     console.log( 'The office is closed' ); // it is the weekend
}
else
      console.log('Welcome');
