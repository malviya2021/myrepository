var i = 3;
// we can write while (i!=0) as simply while(I)
//as any value will be converted to boolean equivalent
while (i) { // when i becomes 0, the condition becomes false and the loop stop
    console.log( i );
    i--
}